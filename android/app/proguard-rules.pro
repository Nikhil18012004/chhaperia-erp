# Nothing here is reached by reflection or from JavaScript — there is no
# @JavascriptInterface bridge in this app on purpose, so the default rules
# are enough. Kept as a file because the release build type names it.
