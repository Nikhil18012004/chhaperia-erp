{
  "name": "chhaperia-erp",
  "version": "1.0.0",
  "private": true,
  "description": "Chhaperia ERP — deploy entry. Installs the backend and starts it; the backend talks to MySQL 8.4 (configured via CHHAPERIA_DB_* env vars) and serves the static frontend on the same origin.",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "start": "node backend/src/server.js",
    "export-data": "node tools/erp-export.js",
    "import-data": "node tools/erp-import.js",
    "test": "npm test --prefix backend",
    "postinstall": "npm install --prefix backend --omit=dev"
  }
}
